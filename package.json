{
  "name": "neuron-mv-control",
  "version": "1.0.0",
  "description": "Touch panel controller for EVS Neuron View multiviewers — partial (per-head) snapshot recall only.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=20.15"
  },
  "dependencies": {
    "express": "^4.19.2",
    "undici": "^6.27.0",
    "ws": "^8.18.0"
  }
}
